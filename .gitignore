.DS_Store
/.build
/.swiftpm
/build
/*.xcodeproj
/DerivedData
/out
/MacLimitsTracker.app
/dist
/.codegraph
/.fastembed_cache

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# bd worktrees
.worktrees/

# Team/agent worktrees — иначе git add -A затягивает вложенные репозитории как gitlink
.claude/worktrees/

# OpenCode session state
.omo/

# Кэш моделей fastembed (ygrep) — ~100 МБ бинарников, качается заново
.fastembed_cache/
